import { describe, it, expect } from 'vitest';
import { ServicesComponent } from './services.component';

describe('ServicesComponent', () => {
  it('should create', () => {
    const comp = new ServicesComponent();
    expect(comp).toBeTruthy();
  });
});
